/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'ss3.4sqi.net', 'fastly.4sqi.net'],
  },
};

module.exports = nextConfig;
